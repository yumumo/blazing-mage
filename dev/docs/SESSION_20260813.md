# 本轮整理清单（2026-08-13）

`ASSET_VER = 20260813r`（强刷缓存）。

## 结论（现在以什么为准）

| 项 | 真源 / 约定 |
|---|---|
| 角色样子 | **立绘** `assets/*-portrait.png` |
| 跑步 | `*-run-sheet.png`（本轮**未改**） |
| 跳 / 飞 / 攻 / 滚 | 以立绘为参考重生；散帧 + sheet |
| 尺寸清单 | `assets/sprite-manifest.json` |
| 贴图流程 | 主仓 skill `castle-parkour-art` |

## 本轮改过什么

### 玩法（`js/game.js`）
- 蝙蝠：更慢、更远、锁第 2 层高度
- 障碍密度↑（柱/刺）；坑后安全区更长；坑视觉更深
- 翻滚：贴地 + 矮碰撞 + 护盾光环
- 骑士近战判定加高；传送门更大 + 椭圆判定
- BGM 古堡小调；画布字体改为可读 `Noto Sans SC`
- 角色散帧绘制按内容高统一到站立身高

### 美术资源（`www/castle-parkour/assets/`）
- **金币**：圆形星面币（替换错误金门图）
- **跳/攻/滚**：按立绘重生（法师+战士）；身高对齐 `refH`≈275/277
- **怪物**：`monster-walk-sheet.png` 行走帧
- **奖励空间**：`bonus-bg.png`；`WORLD_ASSETS.bonusBg`
- **跑步表**：保持不动

### UI / 字体
- Google Fonts：`Noto Sans SC` + `Noto Serif SC` + `Cinzel`
- 角色属性行：固定三列网格对齐
- 手机触控 / 安全区再收紧

### Skill / 规则（主仓）
- `.cursor/skills/castle-parkour-art/SKILL.md`：立绘 = likeness 真源
- `references/sprite-animator-notes.md`：sprite-animator 学习笔记 + 失败教训
- `scripts/sheet_template.py`：品红标注格模板
- `.cursor/rules/castle-parkour-art.mdc`：薄路由同步

## 常用命令

```bash
# 品红抠图
python .cursor/skills/immersive-short-video/scripts/chroma_key.py \
  --input core/castle-parkour/www/castle-parkour/assets --glob "*-magenta.png"

# 立绘系动作 sheet 入库（不改 run）
python core/castle-parkour/dev/ingest_motion_from_portrait.py

# 量测清单（sheet frames 会与 measure_sprites merge 保留）
python .cursor/skills/castle-parkour-art/scripts/measure_run_sheet.py \
  --input core/castle-parkour/www/castle-parkour/assets --cols 3 --rows 1 --glob "*-roll-sheet.png"
python .cursor/skills/castle-parkour-art/scripts/measure_sprites.py \
  --input core/castle-parkour/www/castle-parkour/assets
```

## 文件地图

```text
www/castle-parkour/
  assets/                 # 局内真源 PNG + sprite-manifest.json
  js/config/characters.js # ASSET_VER、角色/跑步/翻滚表
  js/config/world.js      # 金币/门/怪走/奖励背景等
  js/game.js              # 玩法与绘制
  css/app.css             # 字体与属性行对齐
dev/
  ingest_motion_from_portrait.py   # 唯一保留的动作入库脚本
  art-raw/                         # 生图原稿（gitignore）
  docs/dev.md                      # 开发文档
  docs/SESSION_20260813.md         # 本文件
```

## 清理掉的杂物
- `dev/_patch_*.py` / `_fix_*.py` / `_ingest_motion_*.py`（除 portrait 入库）等一次性脚本
- skill 内误用的 `*-run-plant-ref.png`（已改立绘为真源）


## 收紧（同日后续）

- 玩法数值抽到 `js/config/gameplay.js`；`game.js` 经 `config/index.js` barrel 导入
- 删除局内未用 sheet（jump/atk/fly）出 `assets/` → 仅留 `dev/art-raw/`
- 合并重复翻滚贴地逻辑；去掉无用 `CASTLE_DISPLAY` / 空壳 `fbSpeed`/`swordRange`
- `.gitignore` 增加 `*-fly-sheet.png`
- manifest 与 assets 对齐（34 张）
