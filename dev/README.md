# 开发备忘（非发布内容）

本目录存放 **Agent / 开发** 用材料，**不是** Cloudflare Pages 发布所需。

发布必需：`www/castle-parkour/`（含 `index.html`、`assets/`、`_headers`）+ 根目录 `wrangler.toml`、面向玩家的 `README.md`。

对照 GitHub 精简仓（仅 `index.html` + `README`）时：多出的本目录、以及主仓 `.cursor/skills` 中的美术流程，均属开发侧。

`art-raw/`：未抠图的 magenta 原稿等，勿拷进 `www/castle-parkour/assets`。
