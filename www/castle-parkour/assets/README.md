# assets/

透明 PNG。流程见主仓 skill **`castle-parkour-art`**。

**尺寸真源**：`sprite-manifest.json`。换图后必须重新 measure。

**局内角色**（注册表在 `../js/config/characters.js`）：
- 跑步 / 翻滚：仅 `*-run-sheet.png`、`*-roll-sheet.png`（不要提交 `*-run1…9.png`）
- 跳 / 攻：`*-jump-ant` / `*-jump` / `*-fly` / `*-atk-wind` / `*-atk`
- 立绘：`*-portrait.png`

**世界道具**（注册表在 `../js/config/world.js`）：金币、火球、怪、火桩等。

**品红底原图**：只放 `dev/art-raw/`（已 gitignore），勿放进本目录。

**障碍火桩**：`fire-pillar.png`（脚底锚定）。
